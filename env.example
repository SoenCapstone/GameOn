# Shared database credentials (used by docker compose + Spring services)
POSTGRES_USER=username
POSTGRES_PASSWORD=password
POSTGRES_DB=gameon_db

# Spring services consult these when loading config via spring-dotenv
DB_USERNAME=${POSTGRES_USER}
DB_PASSWORD=${POSTGRES_PASSWORD}

# Expo frontend (copy these into GameOn-Frontend/.env as well)
EXPO_PUBLIC_API_BASE_URL=http://localhost:8222
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_1234567890
EXPO_PUBLIC_LOG_LEVEL=info

# Frontend dev login for immidiate dev access
EXPO_PUBLIC_DEV_LOGIN_EMAIL=projectEmail
EXPO_PUBLIC_DEV_LOGIN_PASSWORD=password

