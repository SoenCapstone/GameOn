diff --git a/node_modules/sonner-native/lib/commonjs/animations.js b/node_modules/sonner-native/lib/commonjs/animations.js
index 61a65f2..a192ebb 100644
--- a/node_modules/sonner-native/lib/commonjs/animations.js
+++ b/node_modules/sonner-native/lib/commonjs/animations.js
@@ -39,9 +39,6 @@ const getToastEntering = ({
   'worklet';
 
   const animations = {
-    opacity: (0, _reactNativeReanimated.withTiming)(1, {
-      easing: _easings.easeOutCirc
-    }),
     transform: [{
       scale: (0, _reactNativeReanimated.withTiming)(1, {
         easing: _easings.easeOutCirc
@@ -62,7 +59,6 @@ const getToastEntering = ({
     return 0;
   })();
   const initialValues = {
-    opacity: 0,
     transform: [{
       scale: 0
     }, {
@@ -90,9 +86,6 @@ const getToastExiting = ({
     return 50;
   })();
   const animations = {
-    opacity: (0, _reactNativeReanimated.withTiming)(0, {
-      easing: _easings.easeInOutCubic
-    }),
     transform: [{
       translateY: (0, _reactNativeReanimated.withTiming)(translateY, {
         easing: _easings.easeInOutCubic
diff --git a/node_modules/sonner-native/lib/module/animations.js b/node_modules/sonner-native/lib/module/animations.js
index 2e657f1..82eeade 100644
--- a/node_modules/sonner-native/lib/module/animations.js
+++ b/node_modules/sonner-native/lib/module/animations.js
@@ -33,9 +33,6 @@ export const getToastEntering = ({
   'worklet';
 
   const animations = {
-    opacity: withTiming(1, {
-      easing: easeOutCirc
-    }),
     transform: [{
       scale: withTiming(1, {
         easing: easeOutCirc
@@ -56,7 +53,6 @@ export const getToastEntering = ({
     return 0;
   })();
   const initialValues = {
-    opacity: 0,
     transform: [{
       scale: 0
     }, {
@@ -83,9 +79,6 @@ export const getToastExiting = ({
     return 50;
   })();
   const animations = {
-    opacity: withTiming(0, {
-      easing: easeInOutCubic
-    }),
     transform: [{
       translateY: withTiming(translateY, {
         easing: easeInOutCubic
diff --git a/node_modules/sonner-native/src/animations.ts b/node_modules/sonner-native/src/animations.ts
index ae3f23a..92957d1 100644
--- a/node_modules/sonner-native/src/animations.ts
+++ b/node_modules/sonner-native/src/animations.ts
@@ -35,7 +35,6 @@ export const getToastEntering = ({ position }: GetToastAnimationParams) => {
   'worklet';
 
   const animations = {
-    opacity: withTiming(1, { easing: easeOutCirc }),
     transform: [
       { scale: withTiming(1, { easing: easeOutCirc }) },
       {
@@ -59,7 +58,6 @@ export const getToastEntering = ({ position }: GetToastAnimationParams) => {
   })();
 
   const initialValues = {
-    opacity: 0,
     transform: [
       { scale: 0 },
       {
@@ -90,7 +88,6 @@ export const getToastExiting = ({ position }: GetToastAnimationParams) => {
   })();
 
   const animations = {
-    opacity: withTiming(0, { easing: easeInOutCubic }),
     transform: [
       {
         translateY: withTiming(translateY, {
